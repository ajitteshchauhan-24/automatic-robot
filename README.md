# FastAPI Chatbot

This is a Python FastAPI-based chatbot project for US WorldMeds.

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/ajitteshchauhan-24/automatic-robot.git
   cd automatic-robot
   ```

2. Install the dependencies:
   ```bash
   pip install -r requirements.txt
   ```

## Usage

To start the FastAPI server, run:
```bash
uvicorn main:app --reload
```

The API will be available at `http://127.0.0.1:8000`.

## Additional Information

Refer to individual files for more details about functionality and usage.